[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "neuroslice-shared"
version = "1.0.0"
dependencies = [
    "pydantic==2.7.4",
    "redis==5.0.8",
]

[tool.setuptools.packages.find]
where = ["/"]
include = ["shared*"]
